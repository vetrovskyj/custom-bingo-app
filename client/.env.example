# Local dev stays on /api so Vite proxies requests to the backend server
VITE_API_BASE_URL=/api

# For production builds, overwrite this with your deployed API URL
# Example: VITE_API_BASE_URL=https://custom-bingo-api.onrender.com/api
